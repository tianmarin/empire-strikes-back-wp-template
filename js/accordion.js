window.addEvent('domready',function(){
	
/*
	var myAccordion = new Fx.Accordion($$('#wrapper .subPageTitle'), $$('#wrapper .subPageContent'), {
		display: -1,
		alwaysHide: true
	});
	
	var scroll = new Fx.Scroll(document.body, {
		wait: false,
		duration: 500,
		wheelStops: true,
		//offset: {'x': -200, 'y': -50},
		transition: Fx.Transitions.Quad.easeInOut
	});	
	$$('#wrapper .subPageTitle').each(function(el) {
		el.addEvents({
			click: function(e) {
				new Event(e).stop();
				scroll.toElement("wrapper");
			}
		});
	});
	
	//var span = new Element('span',{class:'test'});
	//span.inject($$('.descargas_f a'));

	$$('.descargas_f a').each(function(el) {
		var span = new Element('span',{class:'test'});
		span.inject(el);
	});

*/

/*
	$$('blockquote').each(function(el){
  		var bquoteswitch = new Element('span',{class:'bquote-switch'});
  		var span = new Element('div',{class:'bquote-holder'});
  		bquoteswitch.set('text', 'Expandir Nota!');
		bquoteswitch.inject(el,'before');
		span.inject(bquoteswitch,'after');
  		el.inject(span);
	});
	var myAccordion = new Fx.Accordion($$('.bquote-switch'), $$('.bquote-holder'), {
		display: -1,
		alwaysHide: true,
		
	});
*/


});